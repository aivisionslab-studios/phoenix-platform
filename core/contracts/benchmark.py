from __future__ import annotations
from typing import Protocol, runtime_checkable

@runtime_checkable
class IBenchmarkSDK(Protocol):
    async def run_runtime_benchmark(self, runtime: str, model: str) -> dict: ...
    async def run_disk_benchmark(self, path: str) -> dict: ...
    async def get_last_results(self) -> list[dict]: ...
